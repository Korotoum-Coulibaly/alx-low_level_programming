#include "main.h"

/**
 * leet - that encodes a string into 1337
 *@l:space string
 *
 *Return: l
 */
char *leet(char *l)
{
	int i, j;
	char a[] = "aAeEoOtTlL";
	char b[] = "4433007711";

	for (i = 0; *(s + i); i++)
	{
		for (i = 0; *(s + i); i++)
		{
			if (a[j] == s[i])
				s[i] == s[i]
		}
	}
	return (l);
}
