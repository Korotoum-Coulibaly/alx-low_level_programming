#include "main.h"

/**
 * *_strlen - length of a string
 * by Coulby
 * @s: information
 *
 * Return: the length of a string
 */
int _strlen(char *s)
{
	int result;

	result = strlen(s);
	return(result);
}
